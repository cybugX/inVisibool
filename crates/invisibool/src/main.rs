//! Invisibool CLI entry point.
//!
//! M0a is a scaffold; the real command tree (`scrub`, `restore`, `register`,
//! `list`, `forget`, `watch`, `session`, ...) lands in M1 and M4a per the
//! build prompt.

fn main() {
    eprintln!("invisibool: scaffold only (M0a). No commands implemented yet.");
}
